---
import '@fontsource-variable/unbounded';
import '@fontsource-variable/noto-sans';
// MiSans VF（中文版正文主用，覆盖中文与拉丁；cn-font-split 分块按 unicode-range 按需加载）
import 'misans-vf-4web/dist/result.css';
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE } from '../config/site';
import { getLangFromUrl } from '../i18n/utils';
import type { Lang } from '../i18n/ui';

interface Props {
  title?: string;
  description?: string;
  /** Override the URL-derived locale (e.g. for MDX drafts whose URL doesn't yet match their content language). */
  lang?: Lang;
}

const { title, description = SITE.description, lang: langOverride } = Astro.props;
const lang = langOverride ?? getLangFromUrl(Astro.url);
const siteTitle = lang === 'zh' ? '开启微观宇宙' : SITE.title;
const fullTitle = title ? `${title} · ${siteTitle}` : siteTitle;
---

<!doctype html>
<html lang={lang === 'zh' ? 'zh-CN' : 'en'}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />

    <title>{fullTitle}</title>
    <meta name="description" content={description} />

    <!-- Favicon 套件（RealFaviconGenerator） -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" sizes="96x96" href="/favicon-96x96.png" />
    <link rel="icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta name="theme-color" content="#210a35" />

    <!-- 得意黑 Smiley Sans（中文标题）— cn-font-split 分块子集，按需加载 -->
    <link rel="stylesheet" href="/fonts/smiley-sans/result.css" />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={SITE.url} />
  </head>
  <body>
    <a class="skip-link" href="#main">{lang === 'zh' ? '跳到主内容' : 'Skip to content'}</a>
    <Header />
    <main id="main">
      <slot />
    </main>
    <Footer />
  </body>
</html>
