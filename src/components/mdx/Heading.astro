---
// 装饰标题：开头一个小 icon + 标题文字 + 下面紧贴一条手绘曲线。
// 默认 icon 是手绘四角星；icon / curve 都可传图片路径换成你自己的文件。
// 用法：<Heading level={2}>标题</Heading>  或  <Heading level={3} icon="/icons/x.svg">标题</Heading>
interface Props {
  level?: 2 | 3;
  icon?: string;
  curve?: string;
  /** Optional stable anchor id (else the article TOC auto-slugs from the text). */
  id?: string;
}
const { level = 2, icon = '/icons/star-4.svg', curve, id } = Astro.props;
const Tag = `h${level}` as 'h2' | 'h3';
---

<Tag id={id} class:list={['deco-h', `deco-h--${level}`]}>
  {icon && <span class="deco-h-icon" style={`-webkit-mask-image: url(${icon}); mask-image: url(${icon});`} aria-hidden="true" />}
  <span class="deco-h-body">
    <span class="deco-h-text"><slot /></span>
    {curve ? (
      <img class="deco-h-curve" src={curve} alt="" />
    ) : (
      <svg class="deco-h-curve" viewBox="0 0 300 6" preserveAspectRatio="none" aria-hidden="true">
        <path d="M2,3 C22,1 42,5 70,3 C98,1 124,5 154,3 C184,1 210,5 242,3 C266,1.5 286,4.5 298,3" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" />
      </svg>
    )}
  </span>
</Tag>

<style>
  .deco-h { display: flex; align-items: flex-start; gap: 10px; line-height: 1.12; color: var(--c-pink); }
  .deco-h--3 { color: var(--c-purple); }
  .deco-h-icon {
    flex: 0 0 auto;
    display: inline-block;
    width: 0.92em; height: 0.92em;
    margin-top: 0.1em;
    background-color: currentColor;
    -webkit-mask-repeat: no-repeat; mask-repeat: no-repeat;
    -webkit-mask-position: center; mask-position: center;
    -webkit-mask-size: contain; mask-size: contain;
  }
  /* body shrink-wraps to the text; padding-bottom reserves room for the curve.
     the curve is absolutely positioned so its own intrinsic (viewBox) width
     can't inflate the box — the box stays exactly as wide as the title text. */
  .deco-h-body { flex: 0 1 auto; min-width: 0; width: fit-content; max-width: 100%; position: relative; padding-bottom: 8px; }
  .deco-h-text { display: block; color: var(--c-ink); }
  /* curve = 100% of the (text-width) body, so it grows/shrinks with the title */
  .deco-h-curve { position: absolute; left: 0; bottom: 0; width: 100%; height: 7px; }
  .deco-h--3 .deco-h-curve { height: 6px; }
</style>
